import { NamevariationPipe } from './namevariation.pipe';

describe('NamevariationPipe', () => {
  it('create an instance', () => {
    const pipe = new NamevariationPipe();
    expect(pipe).toBeTruthy();
  });
});
